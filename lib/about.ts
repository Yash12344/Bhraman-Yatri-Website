/**
 * About page content, held verbatim in `data/about.json` exactly as the
 * operator supplied it — headings, emoji and figures included — so the page
 * can be reworded without touching code.
 *
 * The four figures under "Our Journey At A Glance" are written out in that
 * file rather than counted from the trek data, at the operator's request.
 * They need updating by hand when treks are added.
 */
import about from "@/data/about.json";

export const ABOUT = about;
